server.port=5000
spring.application.name=qma-auth
server.forward-headers-strategy=native
server.tomcat.remoteip.remote-ip-header=x-forwarded-for
server.tomcat.remoteip.protocol-header=x-forwarded-proto
server.tomcat.remoteip.port-header=x-forwarded-port

# Remote Aiven Database (Commented out)
# spring.datasource.url=${QMA_AUTH_DB_URL:${QMA_DB_URL:jdbc:postgresql://qma-3c9ee81a-qma-06052026.i.aivencloud.com:22985/defaultdb?sslmode=require}}
# spring.datasource.username=${QMA_AUTH_DB_USERNAME:${QMA_DB_USERNAME:avnadmin}}
# spring.datasource.password=${QMA_AUTH_DB_PASSWORD:${QMA_DB_PASSWORD:AVNS_wnVjkTVNyu3Igttd7RT}}
# spring.datasource.driver-class-name=org.postgresql.Driver

# Production Database (Aiven/Render)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



# Local Database (Backup)
# spring.datasource.url=jdbc:postgresql://localhost:5432/quantity_measurement
# spring.datasource.username=postgres
# spring.datasource.password=root


spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

app.jwt.secret=${QMA_JWT_SECRET:change-me-to-a-long-random-secret-key-at-least-32-bytes}
app.jwt.expiration-ms=${QMA_JWT_EXPIRATION_MS:3600000}
app.auth.cookie.secure=${QMA_COOKIE_SECURE:true}

# Production: https://quantitymeasurementapp-p0gz.onrender.com/oauth/callback
app.oauth2.redirect-uri=${QMA_OAUTH2_REDIRECT_URI:http://localhost:5173/oauth/callback}
# Production: https://quantitymeasurementapp-p0gz.onrender.com
app.oauth2.frontend-origin=${QMA_FRONTEND_ORIGIN:https://quantitymeasurementapp-p0gz.onrender.com}
app.cors.allowed-origins=${QMA_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:4000,http://localhost:3000,http://localhost:8000,http://127.0.0.1:8000,http://127.0.0.1:5173,https://quantitymeasurementapp-p0gz.onrender.com}
app.oauth2.google.client-id=${GOOGLE_CLIENT_ID:332652104232-bjke91gs5g6tj9357gibsu95hpa6h5rn.apps.googleusercontent.com}
app.oauth2.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
# Use standard Spring Security OAuth2 properties for redirection

# Standard Spring OAuth2 registration (used by auto-config)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:332652104232-bjke91gs5g6tj9357gibsu95hpa6h5rn.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:4000/login/oauth2/code/google
spring.security.oauth2.client.registration.google.scope=openid,profile,email

logging.level.org.springframework.security=INFO
logging.level.org.springframework.security.oauth2=DEBUG
# Production Eureka Registry
eureka.client.serviceUrl.defaultZone=https://qma-eureka-kuee.onrender.com/eureka/

# Local Eureka Registry (Backup)
# eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

